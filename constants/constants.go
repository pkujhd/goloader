package constants

// string match prefix/suffix
const (
	FunctionWrapperSuffix = "-fm"
	StkobjSuffix          = ".stkobj"
	FileSymPrefix         = "gofile.."
	OsStdout              = "os.Stdout"
	GOTPCRELSuffix        = "·f"
)

const EmptyString = ``
const ZeroByte = byte(0x00)

const (
	InvalidOffset = int(-1)
)
